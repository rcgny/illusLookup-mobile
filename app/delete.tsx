import { Text, View } from 'react-native';

/**
 * Placeholder Delete screen.
 *
 * This route will eventually display a searchable selection list and a
 * confirmation step before deleting a SQLite record.
 *
 * @returns {JSX.Element} The Delete route placeholder UI.
 */
export default function DeleteScreen() {
	return (
		<View style={{ padding: 20 }}>
			<Text style={{ fontSize: 20 }}>Delete Screen</Text>
		</View>
	);
}
